# Orchestration configuration
# Prefer editing this file instead of root .env for the pipeline knobs

# Campus / cursus defaults
ORCHESTRA_DEFAULT_CAMPUS_ID=76
CURSUS_ID=21

# Fetch cadence
MIN_FETCH_AGE_SECONDS=3600
KEEP_DAYS=30

# Bootstrap modes: empty | raw
ORCHESTRA_DB_BOOTSTRAP_MODE=empty
ORCHESTRA_DB_RAW_PATH=/srv/42_Network/phase2_users_v2_all.json
# todo import with .sql

# Skip metadata fetch (1 to skip)
ORCHESTRA_SKIP_METADATA_FETCH=0
